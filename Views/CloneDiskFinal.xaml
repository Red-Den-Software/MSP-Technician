<UserControl x:Class="msptool.Views.CloneDiskFinal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:msptool.ViewModels"
             xmlns:views="clr-namespace:msptool.Views"
             mc:Ignorable="d"
           
             d:DesignHeight="390" d:DesignWidth="560" Height="390" Background="Transparent" d:DataContext="{d:DesignInstance Type=viewmodels:CloneDiskFinalViewModel}">
    <Border CornerRadius="25" Background="#1D1D1D">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />


            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="30*"/>

            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Grid.ColumnSpan="2">
                <TextBlock HorizontalAlignment="Left" Margin="20,0,0,0" TextWrapping="Wrap" Text="Disk Clone In Progress" VerticalAlignment="Center" FontSize="20" Foreground="White" FontWeight="Bold" FontFamily="Calibri"/>
            </Grid>
            <Grid Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="2">
                <Border CornerRadius="4" BorderBrush="Transparent" BorderThickness="0" Background="#31394A" Width="406" Height="220" Margin="77,10,77,104" >
                    <StackPanel x:Name="stackPanel">
                        
                    </StackPanel>
                
                </Border>
                <ProgressBar x:Name="progressBar" HorizontalAlignment="Left" Height="25" Margin="80,250,0,0" VerticalAlignment="Top" Width="400" Value="{Binding CopyProgress}" Maximum="100" Minimum="0"/>
                <Button Grid.Row="3" Grid.Column="3" Content="Proceed" Style="{StaticResource dataviewbut}" Height="30" Width="90" Margin="400,280,0,0" FontFamily="Calibri" FontSize="20" FontWeight="Bold" VerticalContentAlignment="Center" Command="{Binding CloneCommand}"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>
