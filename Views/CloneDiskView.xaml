<UserControl x:Class="msptool.Views.CloneDiskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:msptool.ViewModels"
             xmlns:views="clr-namespace:msptool.Views"
             mc:Ignorable="d" 
             d:DesignHeight="390" d:DesignWidth="560" Height="390" Background="Transparent" d:DataContext="{d:DesignInstance Type=viewmodels:CloneDiskViewModel}">


    <Border CornerRadius="25" Background="#1D1D1D" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />

            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="30*"/>
                <RowDefinition Height="8*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Warning: Cloning a disk will overwrite all data on the destination disk." FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="GhostWhite" Grid.ColumnSpan="2" Grid.Row="0"/>
            <Button Grid.Column="0" Grid.RowSpan="2" Width="180" Height="210" Style="{StaticResource dataviewbut}" Click="setsource_click" CommandParameter="SetSourceDisk" Margin="0,-40,0,0">
                <StackPanel>
                    <Image Source="/Assets/HardDrive_4x.png"/>
                    <TextBlock Text="Source Disk" FontSize="20" FontFamily="Calibri" FontWeight="Bold" Foreground="GhostWhite" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button Grid.Column="1" Grid.RowSpan="2" Width="180" Height="210" Style="{StaticResource dataviewbut}" Margin="0,-40,0,0">
                <StackPanel>
                    <Image Source="/Assets/HardDrive_4x.png"/>
                    <TextBlock Text="Destination Disk" FontSize="20" FontFamily="Calibri" FontWeight="Bold" Foreground="GhostWhite" HorizontalAlignment="Center" />
                </StackPanel>

            </Button>
            <Border Grid.Column="0" Grid.Row="1">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <Border Background="#252c3B" CornerRadius="4" Padding="5" Margin="0,0,0,30" BorderBrush="{Binding borderBrush}" BorderThickness="{Binding borderThick}">
                        <TextBlock Text="{Binding source_disk}" FontSize="20" FontWeight="Bold" Foreground="GhostWhite" HorizontalAlignment="Center" Background="Transparent" FontFamily="Calibri" />
                    </Border>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Grid.Row="1">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <Border Background="#252c3B" CornerRadius="4" Padding="5" Margin="0,0,0,30">
                        <TextBlock Text="{Binding dest_disk}" FontSize="20" FontWeight="Bold" Foreground="GhostWhite" HorizontalAlignment="Center" Background="#252c3B" FontFamily="Calibri" />
                    </Border>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Grid.Row="2">
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button Content="Start Clone" Style="{StaticResource homeviewBtn}" Width="100" Height="30" Margin="0,0,20,15"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
